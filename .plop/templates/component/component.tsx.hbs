import { {{pascalCase name}}Params } from "./{{pascalCase name}}.interface";

export default function {{pascalCase name}}({ title }: {{pascalCase name}}Params) {
  return (
    <div>
      <h2>{title}</h2>
    </div>
  );
}
